from flask_jwt_extended import JWTManager

# После создания app
app = Flask(__name__)
app.config['JWT_SECRET_KEY'] = 'your-secret-key-here'  # В продакшене используйте надежный ключ
app.config['JWT_ACCESS_TOKEN_EXPIRES'] = timedelta(days=7)

jwt = JWTManager(app)