import { useDispatch } from 'react-redux';
import { showAuthForm, hideAuthForm } from './authSlice';

// Показать форму
dispatch(showAuthForm());

// Скрыть форму
dispatch(hideAuthForm());




const { isFormVisible } = useSelector((state) => state.auth);

isFormVisible={isFormVisible}